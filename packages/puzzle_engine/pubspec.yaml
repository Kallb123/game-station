name: puzzle_engine
description: >-
  Deterministic Sudoku generation and solving for Zibo Games. Pure Dart, no
  Flutter and no I/O, so its tests run in about a second without a device.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
