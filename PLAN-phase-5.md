# Phase 5 — polish

**Not started.** [`PLAN.md`](PLAN.md) is the source of truth for scope and phase order; §7 there
carries this phase's entry and its done-criterion, and §2, §3.7, §5.2, §6 and §8 carry the parts of
this design the rest of the app has to agree with — all five updated in the commit that added this
file, because a plan that contradicts the document it defers to is two documents nobody can trust.
What this phase learns while building it goes into `PLAN.md` §7 when the phase closes (PR 8).

The plan for the four things that turn six working screens into a shipping app: sound, haptics, an
accessibility pass, and a layout that survives rotation and a tablet.

**Three deviations from `PLAN.md` §7, decided before this plan was written:**

1. **`flutter_soloud` is out.** It resolves `http` into the shipped graph at every version — 2.1.7,
   3.5.4 and 4.1.7 all declare `http: ^1.3.0` — and `tool/check_offline.dart` fails on `http`.
   `minisound 3.0.1` replaces it: a clean graph, the same six platforms. §3.1 has the resolution
   table, and the swap is why this phase is planned before it starts rather than during it.
2. **No music anywhere.** §7 asks for "sound effects and light music"; the owner's instruction is no
   music at all — what the arcade gets is a *sound set*, generated by the same script as the Sudoku
   one: shoot, hit, enemy shoot, enemy hit, enemy move, the special enemy, and two more §4.1 argues
   for. `settings.music` therefore keeps neither a control nor a consumer, and its default stays
   `false` (§4.6).
3. **No i18n scaffolding.** §7 asks for `.arb` files with English first. Dropped on the owner's
   instruction — no translation is planned, and an `.arb` pipeline with one locale in it is a build
   step and a lookup indirection bought against a need nobody has. §2 records what changes if a
   second language is ever wanted.

**Contents:**
[1 Scope and constraints](#1-scope-and-constraints) ·
[2 Non-goals](#2-non-goals) ·
[3 Approach](#3-approach) ·
[4 Design](#4-design) ·
[5 Repository layout](#5-repository-layout) ·
[**6 Pull requests →**](#6-pull-requests) ·
[7 Risks](#7-risks) ·
[8 Verification checklist](#8-verification-checklist) ·
[9 Open questions](#9-open-questions) ·
[10 Starting order](#10-starting-order)

---

## 1. Scope and constraints

| Constraint | Rationale and mechanism |
|---|---|
| The audio dependency drags in no network package | `PLAN.md` §2 bans `http` outright and `tool/check_offline.dart` reads the resolved graph. Settled before this plan: `minisound 3.0.1` adds four packages and no `http`; `flutter_soloud`, `audioplayers`, `assets_audio_player` and `soundpool` all resolve `http`. §3.1 has the table |
| The installed APK requests no permission at all | `tool/check_apk_permissions.sh` fails on any `android.permission.*`, and `android-apk.yml` runs it on the release APK as the Android leg of CI. `minisound_ffi`'s own manifest declares `RECORD_AUDIO` for its recorder, which manifest-merges into the app — so the app's manifest removes it with `tools:node="remove"` (§3.2). A children's app that asks for the microphone is a listing liability and a broken promise; here it is a red build |
| Nothing in the app can record | The removal above is the platform half. The source half is a test asserting `lib/` never imports `package:minisound/recorder.dart` or `rec.dart`, modelled on `test/no_random_test.dart` — so reaching for the API fails before the manifest does |
| Audio failure is silence, never a crash | `AppAudio.init` catches everything and latches into a silent state (§4.2). Linux without ALSA or PulseAudio, a locked audio device, a CI machine with no sink: a child's game must play without sound, and `AGENTS.md` forbids surfacing an internal error to the player |
| Nothing plays with `settings.sound` false | Checked in `AppAudio`, not at the call sites: one place to test, and a new call site cannot forget. It is the only audio setting this phase consumes — there is no music, so `settings.music` gates nothing and gains no control (§4.6). A widget test asserts a muted run produces an empty motif log |
| Nothing on the launch path waits for audio | `PLAN.md` §9's two-second cold start. `Player.init` and the fifteen `loadSoundAsset` calls happen on the first play request, not in `main` — the first tick may be late, and a late tick is cheaper than a slow launch |
| The simulation stays clock-free, RNG-free and Flutter-free | `PLAN-phase-4.md` §3, enforced by `tool/check_determinism.dart` and `test/no_random_test.dart`. `InvadersSim` gains an append-only event buffer and a `drainEvents()`; it never calls the audio layer, so `invaders_sim_equivalence_test.dart` and the 60/144 Hz guarantee are untouched |
| Every sound is generated by a committed script | `app/assets/audio/README.md` and `assets/LICENSE-ASSETS.md`: arithmetic in `tool/audio/generate_motifs.py`, no sample library, no third-party licence in the tree. `--check` re-renders and compares bytes, so a hand-edited asset fails |
| Haptics never add a permission | Every `HapticFeedback` method routes through `View.performHapticFeedback`, none through `Vibrator` (§3.5 — corrected mid-PR from a first belief that two of them did). `haptics_call_site_test.dart` keeps every reference inside `core/haptics.dart`, and the APK check would still catch a permission arriving some other way |
| Haptics only where there is a motor | `defaultTargetPlatform` in `TargetPlatform.android \|\| .iOS`, the check already at `settings_screen.dart:191`, moved to `core/haptics.dart` so three call sites share one answer rather than copying it |
| No control signals its state by colour alone | `PLAN.md` §7's accessibility rule. Selection is an `AppBorders.selected` 3 dp ring plus an icon or a label everywhere it appears; per-screen widget tests assert the ring, and `tokens.dart`'s `AppPalette` doc already commits to it |
| Every screen is usable at 200% text scale | A sweep test pumps every route at `TextScaler.linear(2.0)` and fails on any layout exception — a `RenderFlex` overflow throws in a test, so `tester.takeException()` is the assertion. `ScreenScaffold` already avoids `AppBar` for this reason |
| Every screen survives rotation | The same sweep, at three window sizes covering portrait, landscape and a tablet. Nothing in the app locks orientation today, so this is a promise the code has never been asked to keep |
| A tablet does not stretch the Sudoku grid | The board is capped and centred rather than filling a 10" screen: a 240 mm grid means a child's arm travels the width of the table for one digit. `SudokuGridView` already draws square from the shorter side; the cap is a `maxWidth` above it (§4.8) |
| Tap targets stay at their floors in every layout | `AppTapTargets.min` 56 dp and `.primary` 72 dp, already enforced through `AppTheme`. The landscape layouts re-assert them per control in tests, because a `Row` in a 320 dp-tall window is where a floor gets quietly broken |
| `tool/verify.sh` green before every commit | It is what CI runs, in the same order (`AGENTS.md`). Measured at this phase's planning: 490 app tests in 37 s. The sweep tests here pump whole screens, which is the expensive kind of test, so the phase budgets **under 60 s** for `flutter test` and states what it costs when it closes |

---

## 2. Non-goals

| Not doing | Why |
|---|---|
| Music of any kind — a bed under a screen, a menu theme, or a marching loop in the arcade | The owner's instruction, and it agrees with the audience: a loop playing in a room with other people in it is the first thing a parent turns off. The arcade original's four-note descending figure was drafted for this plan and cut with the rest: cycling four pitched notes on the aliens' step is music by another name, and what the phase ships is one **enemy move** sound instead (§3.4) |
| A volume slider, or per-category volumes | One switch, `sound`, and the device's own volume keys. A mixer is a settings screen a six-year-old can break; per-motif levels are set in the synthesiser instead (`assets/audio/README.md`) |
| A sound on every bunker hit | `_resolveShotsVsBunkers` fires on most stray shots from both sides, so it would be the most-played sound in the game and it says nothing: the erosion is already on screen. Revisit only with a distinct quiet thud, and only if a device pass says the bunkers feel dead |
| `.arb` files, `flutter_localizations`, or any translation | The owner's instruction. Every string in the app is a Dart constant today, and the largely numeric UI keeps a later move cheap: the screens name their strings as public constants (`settings_screen.dart`'s `soundLabel` and friends) because the tests read them, and that is already the list a future `.arb` would be seeded from |
| Text-to-speech, or reading the board aloud | Screen readers do this. Adding a second speech path means owning pronunciation and interruption for two audiences |
| A dedicated colourblind mode | The palette is safe for everyone, or it is not fixed by a mode a parent has to find. Colour never carries state alone (§1), which is the part a mode would have bought |
| Sound for the drawing board | Phase 8 (`PLAN-phase-8.md`), whose pencil has no sound designed for it yet. `assets/audio/README.md` already says so |
| Gamepad support, `gamepads`, or rumble | `PLAN.md` §4.2 puts it after the first release. Rumble is haptics on a device the app has no other reason to know about |
| A soundtrack that reacts to progress in either game | There is no music to react (§3.4), and a puzzle has no beat to hang one on. Inventing one means inventing a music director |
| An emulator job for `integration_test/` | `PLAN.md` §9's open question, inherited not answered. The two smoke tests stay hand-run (`AGENTS.md`), and this phase adds no third |

---

## 3. Approach

### 3.1 Playback: `minisound`, and why not `flutter_soloud`

`PLAN.md` §2 names `flutter_soloud` for audio. Resolving the graph before writing this plan — the same
step `PLAN.md` §10 credits for phase 8's shape — shows it cannot be taken:

| Package | Resolved version | Adds to the shipped graph | Verdict |
|---|---|---|---|
| `flutter_soloud` | 2.1.7, 3.5.4, 4.1.7 | `http`, `http_parser`, `web`, `ffi`, `logging`, `path` | **Rejected.** `http: ^1.3.0` is a direct dependency at every version, for its `loadUrl`. `tool/check_offline.dart` fails on `http` in the runtime closure, and narrowing that check is the one thing `AGENTS.md` says never to do |
| `audioplayers` | 6.8.1 | `http`, `file`, `synchronized`, `uuid`, five platform packages | **Rejected.** Same failure, plus a larger graph. `flame_audio` is rejected with it: it is a wrapper over this |
| `assets_audio_player` | 3.1.1 | `http`, `rxdart`, `uuid` | **Rejected.** Same failure |
| `soundpool` | 2.4.1 | `http` through its web implementation | **Rejected.** Same failure, and no Linux or Windows support to compensate |
| `flutter_pcm_sound` | 3.3.3 | Nothing — `flutter` alone | **Rejected.** The cleanest graph available and only Android, iOS and macOS: no Windows, no Linux. It is also a raw PCM sink, so taking it means writing a real-time mixer in Dart to feed it. Both halves are wrong for a polish phase |
| **`minisound`** | **3.0.1** | `minisound`, `minisound_ffi`, `minisound_platform_interface`, `minisound_web`, and the SDK's `flutter_web_plugins`. No `http` | **Taken.** Android, iOS, Windows, macOS, Linux and web from one package, MIT, and `miniaudio` is vendored in `minisound_ffi/src/external/miniaudio` rather than fetched at build time |

What the API gives, and what it does not:

```dart
final player = Player();                                  // package:minisound/player_flutter.dart
await player.init(32);                                     // periodMs — latency against crackle
await player.start();
final sound = await player.loadSoundAsset('assets/audio/sudoku/place.wav');
sound.volume = 0.7;                                        // per sound, 1.0 default
sound.play();                                              // stop() then resume(): one voice
sound
  ..isLooped = true
  ..loopDelay = Duration.zero;                             // the UFO warble
```

- **One voice per loaded sound.** `Sound.play()` is `stop()` then `resume()`, so a retrigger cuts the
  previous instance. For a placement tick or an alien exploding that is what should happen anyway —
  two aliens dying in the same frame are one explosion. Where it would be wrong is a long sound
  overlapping itself, and there is none in the set (§4.1's longest is the 1.4 s fanfare, and a puzzle
  is solved once).
- **No master volume and no engine-wide pause.** Ducking is therefore this app's job: `AppAudio`
  holds every loaded `Sound` and stops them on background (§4.2).
- **`pitch` exists** and changes speed with it. Not used: every motif is a separate file, so the
  README table stays the documentation instead of a multiplier in Dart deciding what a sound is.
- **Assets are read through `rootBundle.load`**, which is why `assets/audio/` finally gets declared in
  `pubspec.yaml` — the commented-out block there has been waiting for this phase.

### 3.2 `RECORD_AUDIO` has to be removed, not tolerated

`minisound_ffi/android/src/main/AndroidManifest.xml` is three lines, and one of them is
`<uses-permission android:name="android.permission.RECORD_AUDIO" />` — declared for the recorder API
this app never touches. Android's manifest merger pulls it into the app, where two things reject it:
`tool/check_apk_permissions.sh` fails on any `android.permission.*`, and a store listing for a
children's app would show microphone access.

The app's own manifest removes it:

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- minisound_ffi declares RECORD_AUDIO for its recorder, which this app never uses.
         PLAN-phase-5.md §3.2: a children's app requests no microphone, and
         tool/check_apk_permissions.sh fails the build if this line stops working. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove" />
```

`tools:node="remove"` is the merger's own mechanism, and the evidence is the built artifact rather
than the source: CI's `build-android` leg calls `android-apk.yml`, which runs the permission check on
the release APK. If the removal ever stops working — a dependency bump, a merger change — that job
goes red. This is the first pull request's done-criterion for exactly that reason.

iOS and macOS need nothing equivalent: playback touches no protected resource, so no
`NSMicrophoneUsageDescription` and no `com.apple.security.device.audio-input` entitlement. The macOS
sandbox entitlements stay as they are.

### 3.3 Sounds are events out of the model, not calls from the widgets

Both games already own their state in a plain-Dart model — `SudokuSession` and `InvadersSim` — and
both know things the UI above them does not: whether an `enter()` actually changed a cell, whether the
alien block stepped this frame, whether a digit was wrong on a profile that flags wrongness. So each
model emits events and the layer above plays them:

- `SudokuSession` sets a single `SudokuEvent?` slot per mutation, drained in the play screen's
  existing `_onSessionChanged` listener. A no-op `enter()` on a given emits nothing, so the tick
  cannot fire for a tap that did nothing.
- `InvadersSim.step()` appends to a bounded buffer; `InvadersGame.update` drains it after stepping.

The alternative was playing sounds from the call sites — the keypad button, the pad's FIRE listener.
It was rejected on both games for the same reason: the widget knows what was pressed, not what
happened. FIRE held against a cooldown is the clear case — the button is down for 350 ms and one shot
leaves the ship.

Neither model gains a dependency on the audio layer, which keeps the events testable with no
`Player`, no bindings and no assets: a `List<SudokuEvent>` is compared against a scripted sequence of
moves.

### 3.4 The enemy move is a sound, not a loop

The arcade set is eight one-shots and one loop (§4.1), and the one that needs a decision is the
**enemy move**: a step of the alien block, which happens between eleven times a second and once every
1.1 s depending on how the run is going. `InvadersSim._currentStepInterval()` scales
`rules.baseStepForWave(wave)` by the fraction of aliens still alive, clamped to `rules.minStep` —
0.70 s down to 0.09 s on normal, 1.10 s down to 0.20 s on easy.

**One `alienStep` event, one `alien_move` motif, played once per step.** Consequences:

- The pulse accelerates as the formation thins, and nothing in the app computes a tempo to make that
  happen. There is no loop to keep in sync with a simulation that pauses, restarts, drops a backlog of
  up to `maxStepsPerFrame` steps, or ends; the pulse stops when the block stops because the events
  stop.
- The motif must be shorter than the shortest interval it can play at, or the retrigger of §3.1 cuts
  it mid-decay: 0.09 s on normal, so it is **70 ms**.
- Eleven ticks a second is fast. Whether that reads as tension or as a buzz is a question for ears, not
  for this document — §9 carries it, with skipping alternate steps below 0.12 s as the fallback.

**Two alternatives were drafted and dropped.** Cycling four descending pitched variants per step is the
arcade original's figure, and it is what an earlier draft of this plan proposed: dropped because four
notes in rotation is a melody, and the owner's instruction is no music. A looping bass bed with its
playback rate driven by the alive count is the same idea with more machinery — a rate multiplier per
frame, a loop that has to survive pause, restart and the gap between waves — and it is more clearly
music still.

### 3.5 Haptics: a four-rung ladder, none of it needing a permission

`HapticFeedback` is in `flutter/services.dart`, so no dependency. This section first named only
three of its methods — `selectionClick`, `lightImpact`, `mediumImpact` — on the belief that
`vibrate()` and `heavyImpact()` reached Android's `Vibrator`, which needs
`android.permission.VIBRATE`. **That belief was wrong, and PR 5 corrected it after a device pass
found the three-call design too faint to reliably feel.** The engine's own
`PlatformPlugin.vibrateHapticFeedback` (`shell/platform/android/io/flutter/plugin/platform/
PlatformPlugin.java`) routes every one of `HapticFeedback`'s six methods through
`View.performHapticFeedback`, each with its own `HapticFeedbackConstants` value —
`heavyImpact` gets `CONTEXT_CLICK`, the bare `vibrate()` gets `LONG_PRESS` — and none of them touch
`Vibrator`. Nothing here needs a permission.

`core/haptics.dart` therefore climbs all four of `selectionClick`, `lightImpact`, `mediumImpact` and
`heavyImpact` as an escalating ladder (§4.5), rather than stopping at three. The guard that matters
is not which names are used but where: a call to `HapticFeedback` from outside that one file skips
both the mute and the platform gate, so `haptics_call_site_test.dart` asserts every reference lives
in `core/haptics.dart` alone — mirroring `test/no_random_test.dart` down to its self-tests, the same
guard shape the corrected belief no longer needs a permission-based version of.

### 3.6 The load-bearing decision

**`minisound` as the playback layer.** Everything else here is additive to screens that exist: the
event plumbing, the motifs, the haptics and the layout work all stand whatever plays the bytes. If
`minisound` fails a device pass — no sound on an iPhone, a crash on a cheap Android tablet — the
fallback is not another package, because §3.1's table has no clean one left. It is an in-repo method
channel over `SoundPool` on Android and `AVAudioPlayer` on iOS with desktop silent, which is phase
8's picker problem again (`PLAN-phase-8.md` §3.3) and a phase of its own. `AppAudio` is therefore an
interface with the package behind it (§4.2), so the swap is one file — and the risk is front-loaded:
PR 1 puts it on real hardware on all three of Android, a desktop and, if a Mac is available, iOS.

---

## 4. Design

### 4.1 The motif set

Six Sudoku files already exist in `app/assets/audio/sudoku/`, synthesised by
`tool/audio/generate_motifs.py` (`assets/LICENSE-ASSETS.md` has their rows). This phase adds nine
arcade files to the same script — a function each, a row each in `MOTIFS`, in the README's table and
in the licence inventory:

| File | The sound | Plays when | Level | Length |
|---|---|---|---|---|
| `arcade/player_shoot.wav` | shoot | The ship fires | −16 dBFS | 90 ms |
| `arcade/player_hit.wav` | hit | The ship is destroyed | −8 dBFS | 500 ms |
| `arcade/alien_shoot.wav` | enemy shoot | An alien fires | −18 dBFS | 110 ms |
| `arcade/alien_hit.wav` | enemy hit | An alien is destroyed | −12 dBFS | 180 ms |
| `arcade/alien_move.wav` | enemy move | The block steps sideways or down (§3.4) | −16 dBFS | 70 ms |
| `arcade/ufo_loop.wav` | special enemy | While the UFO is on screen, looped | −20 dBFS | 400 ms |
| `arcade/ufo_hit.wav` | special enemy hit | The UFO is destroyed | −8 dBFS | 350 ms |
| `arcade/wave_clear.wav` | — | The last alien of a wave dies | −8 dBFS | 700 ms |
| `arcade/extra_life.wav` | — | The 10,000-point bonus life | −8 dBFS | 600 ms |

**The files and the events are named after `invaders_sim.dart`, not after the play field.** "Alien" and
"UFO" are what the simulation calls them — `AlienBlock`, `Ufo`, `_maybeFireAlien` — so `alien_shoot`
greps from the asset to the line that fires it, where `enemy_shoot` would grep to nothing. The middle
column above is the plain-language name for anyone reading the README rather than the code.

The last two rows are the "possibly others": a **wave clear** because finishing a wave is the only
progress marker Invaders has and a child who cleared one deserves to hear it, and an **extra life**
because a 10,000-point bonus that arrives silently is a bonus nobody notices. `_maybeAwardBonusLife`
and `_checkWaveClear` already detect both, so each costs one event and one motif.

The design rules are the script's existing ones (`generate_motifs.py`'s header): C major at
A4 = 440 Hz so two overlapping motifs still sound like one app, raised-cosine attacks and a 6 ms
fade so nothing clicks, and levels set per motif rather than normalised flat. Five specifics for this
set:

- **`alien_move` is one low note with the `MUTED` timbre**, not a pitched sequence (§3.4) — low enough
  to sit under the shots, short enough that eleven a second do not overlap.
- **The two shoot sounds must not be confusable.** The player's rises, the aliens' falls, and the
  aliens' sits 2 dB quieter: with up to three alien shots in flight (`maxAlienShots`), the sound a
  child needs to react to is the one that is about to arrive, not the one they fired.
- **Explosions are filtered noise, not tones.** `low_pass` over white noise: the script has the
  filter and no noise source, so one is added beside `render`.
- **`player_hit` is the loudest thing here, and still not scary.** `AGENTS.md`: no scary failure
  states. A descending noise sweep, not a klaxon.
- **No `game_over` motif.** The last `player_hit` plus the game-over card is the ending; a second
  sound on top of the card is a sound played over a screen a child is reading.

`python3 tool/audio/generate_motifs.py --check` compares bytes past the 44-byte header, so the script
and the committed files cannot drift. It stays out of `tool/verify.sh` and CI for the reason the icon
script does: the outputs are committed, and no build should need a synthesiser. **The numbers are the
sound — every added motif is listened to before it is committed.**

### 4.2 `AppAudio`: the mute-aware wrapper

`lib/core/audio/` gets three files: the motif enum, the interface with its two implementations, and
the provider.

```dart
/// Every sound in the app, and the asset each one is.
enum Motif {
  sudokuPlace('sudoku/place.wav'),
  sudokuCorrect('sudoku/correct.wav'),
  sudokuWrong('sudoku/wrong.wav'),
  sudokuErase('sudoku/erase.wav'),
  sudokuHint('sudoku/hint.wav'),
  sudokuComplete('sudoku/complete.wav'),
  arcadePlayerShoot('arcade/player_shoot.wav'),
  arcadePlayerHit('arcade/player_hit.wav'),
  arcadeAlienShoot('arcade/alien_shoot.wav'),
  arcadeAlienHit('arcade/alien_hit.wav'),
  arcadeAlienMove('arcade/alien_move.wav'),
  arcadeUfoLoop('arcade/ufo_loop.wav'),
  arcadeUfoHit('arcade/ufo_hit.wav'),
  arcadeWaveClear('arcade/wave_clear.wav'),
  arcadeExtraLife('arcade/extra_life.wav');

  const Motif(this.asset);

  /// The file under `assets/audio/`.
  final String asset;
}

/// No `isMusic`, because there is no music: `settings.sound` gates all fifteen
/// (§4.6).

abstract interface class AppAudio {
  /// Plays [motif] once, at [gain] times its own level. Silent when the
  /// setting that gates it is off, when the engine failed, or on a platform
  /// with no audio device.
  void play(Motif motif, {double gain = 1.0});

  /// Starts [motif] looping until [stopLoop]. Only the UFO warble uses it.
  void startLoop(Motif motif);
  void stopLoop(Motif motif);

  /// Stops everything now — the app going to the background, and a quit.
  void stopAll();

  /// The settings changed: a mute takes effect immediately, mid-puzzle.
  void applySettings(AppSettings settings);
}
```

- **`MinisoundAudio`** is the real one. `Player`, lazily `init(32)`-ed and `start()`-ed on the first
  `play`, a fifteen-entry `Map<Motif, LoadedSound>` filled on demand, and a `_failed` latch.
  Every call is wrapped: any throw sets `_failed`, logs once through `debugPrint`, and every later
  call returns immediately. There is no retry — an audio device that failed at 20:00 is not worth
  asking again about on every placement.
- **`SilentAudio`** is the no-op, and it is what widget tests get by default so no test needs an
  audio device. The tests that assert *what* would play use `RecordingAudio`, a `SilentAudio` with a
  `List<Motif>` behind it (`test/core/audio/recording_audio.dart`).
- **The setting is checked here.** `play` and `startLoop` return early when `!sound`, so a call site
  cannot forget, and `applySettings` stops the UFO loop the moment **Sounds** goes false rather than
  waiting for the UFO to leave.
- **Backgrounding stops everything.** `app.dart` already holds an `AppLifecycleListener` for the save
  flush; `onPause` and `onDetach` gain `stopAll()`. The one-shots are all under a second, so what this
  really stops is the UFO warble — the sound that would otherwise still be playing when a parent
  switches to a phone call.
- **`ProviderScope` owns it**: `appAudioProvider`, overridden with `SilentAudio` in
  `test/app_harness.dart`, which is where every widget test already builds its scope.

Loading is 15 files of a few hundred kilobytes in total, `rootBundle` reads them lazily, and none of
it is on the launch path (§1).

### 4.3 Sudoku call sites

`SudokuSession` gains one nullable field and one drain:

```dart
enum SudokuEvent { placed, placedCorrect, placedWrong, noted, erased, hinted, restored, solved }

/// What the last mutation did, for the screen to turn into a sound. Cleared by
/// the read: an event is played once.
SudokuEvent? takeEvent();
```

The mapping, asserted in `sudoku_session_test.dart` against both `MistakeFeedback` values:

| Mutation | `MistakeFeedback.immediate` | `MistakeFeedback.atCompletion` |
|---|---|---|
| `enter(d)` writing a digit | `placedCorrect` / `placedWrong` | `placed` |
| `enter(d)` toggling a pencil mark | `noted` | `noted` |
| `enter(d)` that changes nothing | nothing | nothing |
| `erase()` that empties a cell | `erased` | `erased` |
| `hint()` pointing at a wrong cell | `hinted` | `hinted` |
| `hint()` revealing a cell | `hinted` | `hinted` |
| `undo()` / `redo()` | `restored` | `restored` |
| `select(i)` | nothing | nothing |
| The move that completes the grid | `solved` | `solved` |

One slot, so one sound per move, and `solved` is the move's event rather than a second one: the
trumpet replaces the placement tick on the digit that finishes the grid instead of landing 20 ms
after it.

Then `Motif` follows from the event: `placed` and `noted` play `sudokuPlace` (`noted` at
`gain: 0.7` — a pencil mark is a lighter version of the same act), `restored` plays `sudokuErase`,
and the rest map by name.

**One sound per placement, not two.** `placedCorrect` plays `correct` *instead of* `place`, where
`assets/audio/README.md` can be read as asking for both. Two motifs 150 ms apart on every digit is
mush, and their levels were set to be heard alone. The README is corrected in the same pull request —
it is the document that carries this rule, and two documents disagreeing is worse than either.

**`atCompletion` never hears `correct` or `wrong`.** That is the whole point of the mode
(`PLAN.md` §3.7): a placement sound that brightened for a right digit would hand back through the
speaker what the screen is deliberately withholding. The session decides it, because the session is
what already knows `mistakeFeedback`, and a test asserts a full wrong-digit sequence on an
`atCompletion` profile logs nothing but `sudokuPlace`.

`solved` fires the trumpet under the confetti — `PLAN.md` §3.7's "playing the sound is phase 5",
closed here. It plays whatever `reduceMotion` says: a child who asked for less movement did not ask
for less celebration.

### 4.4 Invaders events

```dart
enum InvadersEvent {
  playerShot, playerKilled, alienShot, alienKilled, alienStep,
  ufoAppeared, ufoLeft, ufoKilled, waveCleared, extraLife,
}

/// Everything that happened since the last drain, oldest first. Capped at
/// [maxBufferedEvents] (64) with the oldest dropped, so a sim stepped by a test
/// that never drains cannot grow without bound.
List<InvadersEvent> drainEvents();
```

Emitted from the nine methods that already detect these things — `_updatePlayer` (the shot),
`_maybeFireAlien` (the alien shot), `_advanceAliens` (the step), `_resolveShotsVsAliens` (the alien
and the UFO killed), `_resolveShotsVsPlayer` and `_resolveBlockVsPlayerRow` (the ship),
`_advanceUfo` (arrival and departure), `_checkWaveClear`, `_maybeAwardBonusLife`. No new state and no
new branch: each is one `_events.add` beside a mutation that is already there.

`InvadersGame.update` drains after its step loop, inside the existing `if (steps > 0)` block, and maps
each event to its `Motif` one-for-one — `ufoAppeared` and `ufoLeft` to `startLoop`/`stopLoop`, the
other eight to `play`. `pause()` calls `stopAll()`, so a paused game is silent including the UFO.

Determinism is unaffected: events are output only, nothing in `step()` reads them, and
`invaders_sim_equivalence_test.dart` compares simulation state. A dropped backlog
(`maxStepsPerFrame`) drops the events with it, which is right — the frames those sounds belonged to
were never drawn.

The last alien of a wave emits both `alienKilled` and `waveCleared`, and both play: two motifs 180 ms
apart, the explosion under the fanfare. Unlike a Sudoku placement (§4.3) these are two events rather
than one moment described twice, and the arcade is the loud screen. The same goes for a shot that
kills the UFO while the block is stepping — three voices, three motifs, and §3.1's one-voice rule
only ever cuts a motif off against *itself*.

### 4.5 Haptics

`lib/core/haptics.dart`: `AppHaptics` with the same shape as `AppAudio` — an interface, a real
implementation over `HapticFeedback`, a `SilentHaptics` for tests, a provider, and the
`AppSettings.hapticsLevel` check inside rather than at the call sites. `_deviceCanVibrate` moves here
from `settings_screen.dart:191` as the public `deviceCanVibrate`, which the settings screen then
imports; the settings row keeps hiding itself on desktop for the reason `PLAN-phase-1.md` §4.5 gives.

**Revised mid-PR, after a device pass.** The section below through §3.6's original three-call design
shipped first and felt too faint on a Pixel 6 to reliably notice — §3.5's corrected finding that none
of `HapticFeedback`'s six methods need a permission is what freed the fix. `AppHaptics` keeps three
methods, but named for the event rather than for a platform call, because which call answers each one
now moves with the level:

```dart
abstract interface class AppHaptics {
  void tap();      // A digit or a note entered, or a pad button pressed
  void mistake();  // A wrong digit on an `immediate` profile
  void impact();   // The ship destroyed, and game over
  void applySettings(AppSettings settings);
}
```

A fourth setting value, `HapticsLevel { off, low, medium, high }`, replaces the plain on/off
`AppSettings.haptics` bool: the settings screen draws it as a slider rather than a switch, and
`SystemHaptics` climbs a four-rung ladder — `selectionClick`, `lightImpact`, `mediumImpact`,
`heavyImpact`, lightest first — rather than a fixed call per event.

| Event | Rung at `low` | Why that starting rung |
|---|---|---|
| `tap` | 0 (`selectionClick` → `lightImpact` once `low`'s offset is added) | The lightest event, fired hundreds of times a puzzle |
| `mistake` | 1 | The one place a child should feel a difference from `tap`, and still not a punishment. One buzz per tap, the same rule as one sound per move (§4.3) |
| `impact` | 2 | The two moments in an Invaders run worth feeling — the ship destroyed and the run ending, one `InvadersEvent.playerKilled` case covering both |

Each `HapticsLevel` above `off` adds an offset to an event's own rung — `low` adds 1, `medium` adds 2,
`high` adds 3 — clamped to the ladder's top rung (`heavyImpact`). `low`'s offset of 1 is the "turn it
up one notch" fix a device pass asked for, applied before the slider ever leaves its default: what
shipped first as `tap` → `selectionClick` is `tap` → `lightImpact` now. Where the wanted rung would
run past the ceiling, `SystemHaptics` repeats the pulse instead — a short gap, then the same call
again — once per rung it overshot by, which is the only way left for an event already pinned to
`heavyImpact` to keep feeling `HapticsLevel` rise: `impact` at `high` triple-pulses, `mistake` at
`high` double-pulses, `tap` at `high` is a single strongest pulse.

Nothing on a Sudoku completion: the fanfare and the confetti are the celebration, and a buzz under
them reads as an error.

A save written by the three-call design decodes through a compatibility read in `save_codec.dart`:
the old `haptics` bool is gone from what the app writes, but a file still holding it maps `true` to
`HapticsLevel.low` and `false` to `off`, so nothing already using this still-unreleased phase loses
its buzz. `schemaVersion` stays 1 — the app has no shipped users yet for `haptics` to need protecting
against, unlike a field `PLAN.md` §5.2 declares final after a real release.

### 4.6 `settings.music` stays dead, and the settings screen keeps four switches

`AppSettings.music` has existed since schema v1 with no control and no consumer
(`PLAN-phase-1.md` §2), on the understanding that phase 5 would give it both. It gets neither: there
is no music to switch off (§3.4), and `PLAN-phase-1.md` §4.5's rule applies exactly as it did to
**Buzzing** on a desktop — a switch that changes nothing is worse than an absent one, because a
grown-up who finds it will believe they have turned something off.

So the field stays in the schema, unread, at its `false` default. It is not removed:

- Removing it is a shape change to a format whose whole design is that v1 is final
  (`PLAN-phase-1.md` §4.2), and it would cost a migration for a field nothing reads.
- `PLAN.md` §5.2's example block keeps `"music": false`, and the decode test keeps asserting it — a
  field that is written and read back is what proves the codec round-trips, whether or not the app
  acts on it.
- If music is ever wanted, the field is already there and already persisted, which is the reason
  phase 1 declared it in the first place.

The settings screen therefore keeps the four rows it has — **Sounds**, **Buzzing**, **Show the
timer**, **Less moving about** — and gains no fifth for `music`. What changes there is one comment:
`settings_screen.dart`'s "Six controls, and deliberately not seven: `music` is in schema v1 but gets no
control until phase 5" now has to say *why* it still gets none, and a comment that cites a phase which
has been and gone is the kind of stale reasoning `AGENTS.md` asks to be fixed in the same change.
**Buzzing** itself stopped being a switch mid-phase (§4.5) — three of the four rows are still a switch,
the fourth is a slider, and the comment says so rather than calling all four "switches".

### 4.7 The accessibility pass

Much of this is already done, and the plan says which so the phase does not re-derive it: `Semantics`
already wraps the controls on every screen — `grep -rln Semantics lib` names eleven files —
`ScreenScaffold` avoids `AppBar` precisely so a 200% title wraps instead of clipping,
`AppTheme` derives Material's minimums from `AppTapTargets` so a floor cannot be broken by a widget
nobody reviewed, `sudoku_cell.dart` pins its digits to `TextScaler.noScaling` and explains why, and
`reduceMotion` is or-ed into `MediaQuery.disableAnimations` in `app.dart` with `Confetti` already
returning `SizedBox.shrink()` for it.

What this phase adds:

1. **An audit, screen by screen, on a device with a screen reader** — TalkBack on Android and
   VoiceOver on iOS or macOS, walking every route in `AppRoutes`: home, profiles, settings, the
   Sudoku menu, a puzzle, the arcade menu, a run. What it looks for: a control that announces nothing,
   one that announces its state wrongly (a switch that says "Sounds" but not "on"), a decoration that
   announces at all, and a focus order that reads a screen out of the order it is drawn in.
2. **The gaps that audit finds**, as `Semantics` labels, `MergeSemantics` where a row reads as three
   things, and `ExcludeSemantics` on decoration. The HUD is the known one: `game_shell.dart` draws
   score, lives and wave as three `_HudText`s that change every frame, and a screen reader
   re-announcing them mid-run is worse than not reading them — they become one merged, `liveRegion:
   false` label.
3. **A contrast test**, not a contrast opinion: a test computing WCAG 2.1 relative luminance for
   every foreground-on-background pair `AppPalette` and the derived `ColorScheme`s produce, asserting
   4.5:1 for body text and 3:1 for large text and control borders, in both brightnesses. This is the
   one accessibility claim in `PLAN.md` §7 that nobody can check by looking, and the palette is where
   a future tweak would break it silently.
4. **A colour-alone sweep**: per-screen assertions that every selected state carries its
   `AppBorders.selected` ring or its icon — the difficulty chips, the theme choices, the mistake-mode
   choices, the pencil-mode toggle, the pad-side toggle.
5. **The text-scale sweep** of §4.8, which is the same test as the rotation sweep with a different
   axis.

### 4.8 Layout: orientation, tablets, and the sweep that guards both

Nothing in the app locks orientation — there is no `SystemChrome` call anywhere in `lib/` — so every
screen already rotates, and nothing has ever checked what happens when it does. Three of them need
work, and all of them need a guard.

`lib/core/ui/layout.dart` holds the one breakpoint, so no screen invents its own:

```dart
/// How much room a screen has, by the short side of the window.
///
/// 600 dp is Material's own compact/medium boundary and the number a "phone or
/// tablet" question actually turns on. Read from `MediaQuery.sizeOf`, not from
/// the physical device: a phone in landscape and a tablet in a split-screen
/// window are the same problem.
enum AppFormFactor { compact, expanded }

/// The widest a column of content is allowed to get: 640 dp.
///
/// A settings list stretched across a 10" tablet is a 900 dp-wide switch whose
/// label is at one end and whose control is at the other.
const double maxContentWidth = 640;

/// The widest the Sudoku board is drawn, however much room there is: 560 dp.
const double maxBoardSide = 560;
```

| Screen | Portrait | Landscape | Expanded |
|---|---|---|---|
| Home (`home_screen.dart`) | Two cards stacked, as now | The two cards side by side in a `Row` — stacked, each card is a 200 dp-tall letterbox | Cards capped at `maxContentWidth` each and centred |
| Sudoku play | Grid above, keypad below, as now | Grid left, keypad right in a `Row`: stacked in a 400 dp-tall window the board is 180 dp and a cell is 20 dp | Board capped at `maxBoardSide`, centred, keypad beside it |
| `GameShell` + `OnScreenPad` | HUD, field, pad band below, as now | The field letterboxes to 7:8 and leaves the sides empty, so the pad goes into that space — LEFT/RIGHT one side, FIRE the other, still never over the field, still swapped as a pair by `padSide` (`PLAN.md` §4.2) | Field capped so the pad stays in thumb reach rather than at the screen's edges |
| Settings, profiles | As now — both already scroll | As now | Content capped at `maxContentWidth` and centred |
| Sudoku menu, arcade menu | As now — both already use `SingleChildScrollView` | As now | Content capped at `maxContentWidth` and centred |

**The guard is one test file**, `test/layout_sweep_test.dart`, pumping every route at every
combination of three window sizes (a 360×640 phone, a 640×360 phone in landscape, an 834×1112
tablet), two text scales (1.0 and 2.0) and both brightnesses. A `RenderFlex` overflow, an unbounded
constraint or a failed assertion throws during a widget test, so the assertion is that
`tester.takeException()` is null — plus, for the Sudoku screen, that the rendered board is square and
its side is at most `maxBoardSide`. Twelve pumps per route is the expensive kind of test, which is
why §1 budgets `flutter test` and why the sweep uses one shared harness rather than one test per
combination.

**Rotation must not restart a run.** `InvadersGame` is held in `_InvadersScreenState`, so a rotation
rebuilds the widget and keeps the game — the sweep asserts the same `InvadersGame` instance and a
non-zero score survive a `MediaQuery` size change, because "the app rotates" and "the run survives
rotating" are two different claims and only the second one matters to a child.

---

## 5. Repository layout

```
game-station/
├─ tool/audio/generate_motifs.py     # + 9 arcade motifs, a noise source, 9 MOTIFS rows
└─ app/
   ├─ assets/audio/
   │  ├─ README.md                   # the arcade table; the placement rule corrected (§4.3)
   │  ├─ sudoku/*.wav                # unchanged
   │  └─ arcade/                     # player_shoot, player_hit, alien_shoot, alien_hit,
   │                                 #   alien_move, ufo_loop, ufo_hit, wave_clear, extra_life
   ├─ pubspec.yaml                   # minisound; assets/audio/ declared at last
   ├─ android/app/src/main/AndroidManifest.xml   # RECORD_AUDIO removed (§3.2)
   ├─ lib/
   │  ├─ core/
   │  │  ├─ audio/
   │  │  │  ├─ motif.dart            # the enum and its asset paths
   │  │  │  ├─ app_audio.dart        # the interface, MinisoundAudio, SilentAudio
   │  │  │  └─ providers.dart        # appAudioProvider
   │  │  ├─ haptics.dart             # AppHaptics, the platform gate, the settings check
   │  │  └─ ui/layout.dart           # AppFormFactor, maxContentWidth, maxBoardSide
   │  ├─ features/sudoku/model/sudoku_session.dart   # + SudokuEvent, takeEvent()
   │  └─ features/arcade/invaders/model/invaders_sim.dart  # + InvadersEvent, drainEvents()
   └─ test/
      ├─ core/audio/                 # the wrapper, the mute gates, RecordingAudio
      ├─ core/haptics_test.dart      # the ladder, the offset, the repeat-at-the-ceiling pulse
      ├─ core/recording_haptics.dart # RecordingHaptics, the haptics half of RecordingAudio
      ├─ haptics_call_site_test.dart # HapticFeedback named only from core/haptics.dart
      ├─ no_recorder_test.dart       # bans minisound's recorder imports in lib/
      ├─ contrast_test.dart          # WCAG ratios over AppPalette (§4.7)
      └─ layout_sweep_test.dart      # rotation and text scale over every route (§4.8)
```

The boundaries: `core/audio/` and `core/haptics.dart` are `core` because three features use them and
none owns them; the event enums live with their models rather than in `core/audio/` because they
describe what happened in a game, not what it sounds like — the mapping from event to `Motif` is in
the feature layer, where a second game can map differently.

---

## 6. Pull requests

Eight, each independently reviewable and each leaving the app working. The order is deliberate: the
dependency and its two platform hazards go first, so the phase's one irreversible risk (§3.6) is
answered on hardware before eight days of work sit on top of it.

**Estimate: 6–8 days, one developer working part time** — the same basis as the phases before it.
`PLAN.md` §7 says 4–5 days; the difference is the dependency swap and its guards (PR 1), the eleven
generated motifs (PR 3), and phase 4's inherited 144 Hz desktop pass (PR 8), against the i18n
scaffolding this phase no longer does. The closing pull request updates §7 with what it took.

### PR 1 — `minisound`, the manifest, and `AppAudio` (1–1.25 day)

`minisound ^3.0.1` in `pubspec.yaml`, `RECORD_AUDIO` removed with `tools:node="remove"` (§3.2),
`assets/audio/` declared, `core/audio/` with both implementations and the provider,
`stopAll()` on the existing lifecycle listener, and exactly one call site: the completion fanfare.

**Done when:** the trumpet plays on solving a puzzle on an Android device and on one desktop target;
turning **Sounds** off mid-puzzle silences it without a restart; `dart tool/check_offline.dart` is
clean with `minisound` in the resolved graph; CI is green on all five build legs and its
`build-android` leg reports **no platform permissions** for the release APK; `test/no_recorder_test.dart`
fails against a source file that imports the recorder and passes against `lib/`; and a widget test
over `RecordingAudio` asserts `Motif.sudokuComplete` on completion and an empty log with
`sound: false`.

### PR 2 — the rest of the Sudoku set (0.5 day)

`SudokuEvent`, `takeEvent()`, and the remaining seven values mapped to motifs in the play screen's
existing `_onSessionChanged` listener — one call site, not six, which is the point of §3.3 — with
§4.3's `MistakeFeedback` rule inside the session. `assets/audio/README.md` corrected for
one-sound-per-placement.

**Done when:** `sudoku_session_test.dart` asserts §4.3's table for both `MistakeFeedback` values,
including that a no-op `enter()` on a given emits nothing; a widget test asserts an `atCompletion`
profile entering wrong digits logs only `sudokuPlace`; and on a device a placement, an erase, a hint
and an undo each sound once and once only.

### PR 3 — the arcade motifs (0.75–1 day)

Nine functions in `generate_motifs.py`, a noise source beside `render`, the README table with its
plain-language column, nine rows in `assets/LICENSE-ASSETS.md`. No Dart.

**Done when:** `python3 tool/audio/generate_motifs.py --check` exits 0 on a fresh checkout; the nine
files are committed; each has been **listened to** on a phone speaker and on headphones, and the pull
request says so; the two shoot sounds are distinguishable from each other played back to back, which
is the one thing a table cannot assert; `alien_move` is under 90 ms, from the script's own printed
length column; and `dart analyze --fatal-infos --fatal-warnings tool` is clean.

### PR 4 — Invaders events and sounds (1–1.25 day)

`InvadersEvent`, the bounded buffer, `drainEvents()`, the emissions in the nine methods, the drain and
the event-to-`Motif` map in `InvadersGame`, and the UFO loop. No settings change: §4.6 is a comment
correction, and it rides along here because this is the pull request that proves there is no music to
switch off.

**Done when:** `invaders_sim_test.dart` asserts the exact event list for a scripted sequence — a shot
fired, an alien firing back, an alien killed, the UFO arriving and leaving, a wave cleared, a life
lost, the bonus life — and that the buffer caps at 64;
`invaders_sim_equivalence_test.dart` still passes unchanged; on an Android device the enemy-move tick
audibly speeds up as the formation thins and stops the instant the game is paused; **Sounds** off
silences all nine; and the UFO warble stops when the app goes to the background, not when it returns.

### PR 5 — haptics (0.5 day, revised to about 1 day after a device pass)

`core/haptics.dart`, `_deviceCanVibrate` moved out of `settings_screen.dart` as the public
`deviceCanVibrate`, the four call sites of §4.5, and `haptics_call_site_test.dart` with its
self-tests.

**A device pass on a Pixel 6 found the first cut too faint to reliably feel** — the three-call
design this pull request first shipped, gated by a plain on/off `AppSettings.haptics`. Checking why
turned up §3.5's correction: none of `HapticFeedback`'s six methods need a permission, which the
original design had assumed of two of them. That freed the fix landed here instead: `HapticsLevel`
replaces the bool, the settings screen draws it as a slider, and `SystemHaptics` climbs the
four-rung ladder §4.5 describes, repeating a pulse once an event's own rung hits the ceiling.

**Done when:** on an Android phone each of the three events is clearly felt at every level from
`low` to `high`, and `off` stops all three; `core/haptics_test.dart` asserts the ladder against the
real platform channel — every level's rung for every event, including the repeated pulses `medium`
and `high` add once an event is pinned to `heavyImpact`, and that `off` and `TargetPlatform.macOS`
each fire nothing; `haptics_call_site_test.dart` fails against a source naming `HapticFeedback`
anywhere but `core/haptics.dart`; a save written by the first three-call design still decodes,
`true` becoming `HapticsLevel.low` and `false` becoming `off`; and the release APK still requests no
permission.

### PR 6 — the accessibility pass (1–1.25 day)

The device audit of §4.7, the labels it finds, the HUD's merged non-live label, `contrast_test.dart`,
and the colour-alone assertions.

**Done when:** every route in `AppRoutes` has been walked with TalkBack on Android and with VoiceOver
on iOS or macOS, and the pull request lists what each pass found and what changed; `contrast_test.dart`
asserts 4.5:1 for body text and 3:1 for large text and borders across both palettes, and fails when a
palette colour is perturbed towards its background; and a test per screen asserts every selected
control carries a ring or an icon rather than only a colour.

### PR 7 — landscape and tablet (1–1.25 day)

`core/ui/layout.dart`, the three screens of §4.8's table, the content-width cap on the other four,
and `layout_sweep_test.dart`.

**Done when:** the sweep pumps every route at three sizes, two text scales and both brightnesses with
no exception; the Sudoku board is square and at most 560 dp in every combination; a run survives a
`MediaQuery` size change with the same `InvadersGame` and its score intact; every pad and keypad
control still measures at least its `AppTapTargets` floor in landscape; and on a real tablet in both
orientations the board is centred rather than stretched and FIRE is reachable by a thumb.

### PR 8 — device pass, the 144 Hz clause, and phase close (0.5–0.75 day)

Ten minutes of Invaders and one full puzzle on an Android phone, on a tablet and on a desktop, with
sound and haptics on and then off; the 144 Hz desktop pass phase 4 left open (`PLAN.md` §7);
`PLAN.md` updated with the outcome — §7's phase-5 entry gains what differed from this plan, and §9's
checklist is ticked where this phase answered it — and the closed banner on this file. The corrections
this plan forced on `PLAN.md` landed **with the plan**, not here: §2's dependency table and banned
list, §3.7's audio sentence, §5.2's note that `music` stays unconsumed, §6's layout, §7's rewritten
entry and 6–8 day estimate, §8's three new risk rows, and §9's APK and rotation items.

**Done when:** `tool/verify.sh` is green and the pull request states the `flutter test` time against
§1's 60 s budget; the same run plays at the same speed on a 60 Hz and a 144 Hz display, measured by
wave reached in a fixed two minutes rather than by impression; the device pass says which hardware
each check ran on; and `PLAN.md` names phase 6 as the next work with nothing from this phase left
open that is not in §9.

**The release line does not move.** Phase 6 still ships (`PLAN.md` §7): this phase is what makes 1.0
worth listing, and phases 7 and 8 remain minor releases after it.

---

## 7. Risks

| Risk | Severity | Mitigation |
|---|---|---|
| `minisound` is one maintainer's package across four packages, and stops being maintained | Medium | It is reached only through `AppAudio` (§4.2), so replacing it is one file — but §3.1's table has no clean alternative left, so the real fallback is the in-repo method channel of §3.6, which is a phase of its own. Front-loaded deliberately: PR 1 puts it on hardware before anything depends on it |
| `RECORD_AUDIO` reaches the APK anyway — the merger changes, or a bump moves the manifest | High | `tools:node="remove"` plus `tool/check_apk_permissions.sh` on the built release APK in CI's `build-android` leg, which fails on any permission. The evidence is the artifact, not the source, and the check already exists |
| The vendor's own README lists iOS and macOS as untested | Medium | 3.0.0's changelog adds GitHub Actions coverage for both and claims to fix the build; CI's `build-apple` leg compiles both from PR 1, and the audit pass in PR 6 needs a VoiceOver device anyway, so playback is checked on one. If it fails there, §3.6's fallback is desktop-silent by design |
| Linux has no ALSA, PulseAudio or PipeWire and `Player.init` throws | Low | `AppAudio` catches and latches silent (§1). `PLAN.md` §8 already called Linux audio flaky and optional; miniaudio loads its backend at runtime, so a missing one is a failed init rather than a failed link |
| The enemy-move tick at the 0.09 s floor buzzes instead of building tension | Medium | A 70 ms motif so nothing is cut mid-decay, and §9's open question with the fallback named: skip alternate `alienStep` events below 0.12 s, which halves the rate and keeps the acceleration audible. Decided by listening in PR 4, not by argument |
| Nine sounds in a busy wave add up to noise — three alien shots in flight, explosions, the move tick | Medium | Levels set per motif and 2 dB of separation between the two shoot sounds (§4.1), one voice per motif so a repeated event cannot stack (§3.1), and the ten minutes of play in PR 8 as the check. The lever if it is too much is the synthesiser, not the code: `alien_shoot` and `alien_move` are the two that fire most and the two cheapest to drop 3 dB |
| One voice per motif swallows simultaneous events | Low | Accepted, and correct for this set: two aliens dying in one frame are one explosion, and no motif in §4.1 is long enough to overlap itself. If a later game needs two, the fix is loading a second `LoadedSound` for that motif — a voice pool in one file |
| Audio init pushes cold start past `PLAN.md` §9's two seconds | Low | Nothing audio touches `main` (§1): `init` and every `loadSoundAsset` run on the first play request. The cold-start check in §9's release checklist is what would catch a regression |
| The sweep test's 12 pumps per route make `flutter test` slow enough that people stop running it | Medium | One shared harness, `SilentAudio` and `SilentHaptics` everywhere, and a stated budget of 60 s that PR 8 reports against. If it does not fit, the brightness axis is what gets dropped — it is the one the other tests already cover |
| Rotating restarts an Invaders run | Medium | `InvadersGame` lives in the screen's `State`, so a rebuild keeps it; asserted in the sweep by checking the instance and the score survive a size change (§4.8) rather than by reasoning about widget lifecycles |
| The accessibility pass is one person's opinion on one device | Medium | Two screen readers on two platforms, and the three claims that can be mechanised are: contrast as a test, tap targets as per-control assertions, text scale as the sweep. What is left to judgement is announcement wording and focus order, and the pull request records what was checked |
| The phase grows: a mixer, music, more motifs, a colourblind mode | Medium | §2 names each one as a non-goal with its reason — including the four-note figure this plan drafted and cut — and `PLAN.md` §8's feature-creep row keeps the release line at phase 6 |

---

## 8. Verification checklist

Commands, from the repository root unless stated:

- [ ] `tool/verify.sh` green, and `cd app && flutter test` under 60 s (§1).
- [ ] `dart tool/check_offline.dart` clean with `minisound` in the resolved graph.
- [ ] `cd app && flutter pub deps --style=compact | grep -E ' http | web_socket'` finds nothing outside
      the `integration_test` dev branch.
- [ ] `python3 tool/audio/generate_motifs.py --check` exits 0 on a clean checkout.
- [ ] CI green on all five build legs, and its `build-android` leg printing
      `No platform permissions — in particular, no INTERNET.`
- [ ] `cd app && flutter test test/haptics_call_site_test.dart test/no_recorder_test.dart` — both pass
      against `lib/`, and both fail when pointed at a source that breaks them.
- [ ] `cd app && flutter test test/contrast_test.dart test/layout_sweep_test.dart` pass.

On hardware — each item names the device it ran on in the pull request:

- [ ] An Android phone: a full 9×9 puzzle with every Sudoku sound heard once, then all of them
      silenced by the **Sounds** switch mid-puzzle.
- [ ] An Android phone: ten minutes of Invaders. All nine sounds heard and told apart — the two shoot
      sounds in particular; the enemy-move tick speeds up as the formation thins; the UFO warble starts
      and stops with the UFO; pausing stops everything; **Sounds** off leaves silence.
- [ ] An Android phone: backgrounding mid-run leaves no sound playing, and returning resumes without
      a burst.
- [ ] An Android phone: haptics felt on a digit entry, a wrong digit and a pad press at every
      **Buzzing** level from Low to High, each clearly stronger than the last; Off stops all three.
- [ ] A 10" tablet, both orientations: the Sudoku board centred and capped, not stretched; FIRE within
      thumb reach; no control smaller than its floor.
- [ ] A desktop target: the same run plays at the same speed on a 60 Hz and a 144 Hz display —
      `PLAN.md` §7's phase-4 clause, closed here.
- [ ] A desktop target: no haptics attempted, no audio error surfaced, and the **Buzzing** slider
      absent.
- [ ] Every route walked with TalkBack, and every route walked with VoiceOver: each control announces
      its name and state, decorations announce nothing, and the arcade HUD does not re-announce
      mid-run.
- [ ] Every screen at 200% system text scale on a real phone, in both orientations, with nothing
      clipped and nothing unreachable.
- [ ] Rotating mid-puzzle keeps the board, the notes and the clock; rotating mid-run keeps the score.
- [ ] Airplane mode: everything above still true — the constraint `PLAN.md` §9 opens with.

---

## 9. Open questions

| Question | What would resolve it |
|---|---|
| Does the enemy-move tick at the 0.09 s floor build tension or buzz? | Listening on a phone speaker in PR 4. If it buzzes, skip alternate `alienStep` events below 0.12 s (§7's row) — the rate halves and the acceleration still tracks the formation |
| Does the UFO warble sit under the rest of the set or fight it? | All nine at their §4.1 levels on a real device, in a wave busy enough to overlap them. The fix if it fights is one number, `ufo_loop`'s peak, and it is a re-render of one motif |
| Do the bunkers need a hit sound after all? | The ten minutes of play in PR 8. §2 drops it because it would be the most-played sound in the game and the erosion is already visible; a device pass saying the bunkers feel dead is what would reopen it |
| Does `minisound` play on iOS and macOS, which its own README lists as untested? | CI's `build-apple` leg compiles both from PR 1; playback needs the VoiceOver device PR 6 uses anyway. Unresolved until then, and §3.6 names what happens if the answer is no |
| Does the sweep test belong in CI at 12 pumps per route, or is it a hand-run suite? | PR 7's measured time against §1's 60 s budget. It goes in `test/` if it fits and the brightness axis is dropped if it does not |
| Is a WCAG 4.5:1 body-text ratio achievable across both palettes without changing the brand hues? | `contrast_test.dart` written before the palette is touched, in PR 6. If a pair fails, the tone the scheme derives moves rather than the seed |
| Should the six Sudoku motifs be re-levelled now that they play against real gameplay? | The device pass in PR 2. They were synthesised without ever having been heard in situ, and §4.1's levels are relative — a change is a re-render, not a code change |
| Does `integration_test/` get an emulator job? | `PLAN.md` §9's open question, inherited and still open. This phase adds no integration test, so nothing here depends on the answer |

---

## 10. Starting order

1. **`minisound ^3.0.1` into `app/pubspec.yaml`, `RECORD_AUDIO` removed, and nothing else.** Run
   `dart tool/check_offline.dart`, then build the release APK and run
   `tool/check_apk_permissions.sh` on it. The two platform hazards of §3.1 and §3.2 are answered
   before a line of `AppAudio` exists, and if either fails the phase's shape changes rather than its
   schedule.
2. **`core/audio/` with `SilentAudio` first**, wired into `test/app_harness.dart`, then
   `MinisoundAudio` behind it. Doing the fake first means the 490 existing tests keep passing while
   the real implementation is still failing on a device.
3. **The fanfare as the only call site**, on real hardware, on Android and on one desktop target.
   One sound proves the whole path — init, load, the settings gate, the lifecycle stop — and every
   pull request after it is call sites and layout.
